import { ProjectLinkBuilder } from './project-link-builder';

describe('ProjectLinkBuilder', () => {
  it('should create an instance', () => {
    expect(new ProjectLinkBuilder()).toBeTruthy();
  });
});
